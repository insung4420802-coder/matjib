# 임슐랭 가이드 v17 적용 안내

## 1. 적용 전에 확인할 값

이 수정본은 운영 중인 v16의 아래 두 브라우저 키를 그대로 보존했다.

- `KAKAO_JS_KEY`
- `GOOGLE_MAPS_JS_KEY`

그래도 업로드 전에 현재 GitHub `main`과 수정본의 두 줄이 같은지 한 번 확인한다. 서버 비밀키는 파일에 넣지 않는다.

## 2. 가장 쉬운 GitHub 적용 방법

1. GitHub 저장소의 v16 `v16-result-controls`를 원본으로 `v17-delivery-links` 브랜치를 만든다.
2. 반드시 `v17-delivery-links`가 선택된 상태에서 **Add file → Upload files**를 누른다.
3. 이 v17 폴더 안의 파일과 폴더를 저장소 최상단에 업로드한다.
4. 같은 이름의 파일을 교체하고 새 파일을 추가한다.
5. 커밋 메시지를 `임슐랭 v17 배달앱 연결`로 적고 커밋한다.
6. Pull Request를 만들고 Vercel Preview 주소에서 먼저 테스트한다.
7. 테스트가 끝난 뒤 Pull Request를 `main`에 Merge한다.

새로 추가되는 파일:

- `api/lib/guard.js`
- `package.json`
- `tests/score.test.mjs`
- `APPLY-GUIDE.md`

## 3. Vercel 환경변수

기존 값은 그대로 둔다.

- `NAVER_CLIENT_ID`
- `NAVER_CLIENT_SECRET`
- `ANTHROPIC_API_KEY`
- `ANTHROPIC_MODEL` — 설정하지 않으면 기존처럼 `claude-haiku-4-5`
- `GOOGLE_MAPS_API_KEY`

선택 사항:

- `APP_ACCESS_KEY`: 지인에게 공유할 접근 코드

`APP_ACCESS_KEY`를 설정하면 앱이 최초 API 사용 시 코드를 한 번 묻고 기기에 저장한다. 예측하기 쉬운 4자리 숫자보다는 8자 이상의 단어+숫자 조합을 권장한다. 설정하지 않으면 기존처럼 코드 없이 동작한다.

접근 코드를 켜면 인증 우회를 막기 위해 서버 공유 캐시는 사용하지 않는다. 소수 지인에게만 공유한다면 코드를 켜는 편이 안전하고, 공개 링크이면서 반복 검색 캐시를 우선한다면 설정하지 않은 채 Google 쿼터를 낮게 잡는 편이 낫다.

환경변수를 추가하거나 바꾼 뒤에는 Vercel에서 **Redeploy**한다.

## 4. Google 비용 안전 설정

Google Maps Platform은 2025년 3월부터 월 $200 일괄 크레딧이 아니라 API 항목별 무료 호출 한도를 사용한다.

Google Cloud Console에서 다음을 설정한다.

1. **Billing → Budgets & alerts**에서 월 예산 알림을 만든다.
2. 50%, 80%, 100% 알림을 켠다.
3. **APIs & Services → Places API (New) → Quotas**에서 예상 사용량보다 약간 높은 일일 한도를 정한다.
4. 서버 키는 Places API (New)만, 브라우저 키는 Maps JavaScript API만 허용한다.

지인 공유 규모라면 무료 사용량 안에 머물 가능성이 높지만, `currentOpeningHours`와 `reviews` 같은 필드는 높은 Places 요금 등급을 사용할 수 있으므로 알림과 쿼터를 함께 설정하는 것이 안전하다.

## 5. 배포 후 확인 항목

### v18 배달 기능

1. 국내에서 `배달 떡볶이`를 검색해도 배민·쿠팡이츠 버튼이 표시되지 않는지 확인한다.
2. 국내 일반 검색은 검증 후기에서 배달 근거가 있는 음식점에만 버튼 없는 `후기에서 배달 언급` 배지가 표시되는지 확인한다.
3. 해외에서 `delivery sushi`를 검색하면 Uber Eats 버튼이 표시되는지 확인한다.
4. 방콕·싱가포르·베트남 등 동남아에서 배달 검색을 하면 Uber Eats 대신 GrabFood가 표시되는지 확인한다.
5. 해외 버튼을 누르면 공식 사이트가 새로 열리고 음식점명과 주소가 복사되었다는 안내가 나오는지 확인한다.

### v16 기능

1. 추천순·가까운 순·후기 많은 순 정렬이 목록에 반영되는지 확인한다.
2. 해외에서 가격 낮은 순과 `₩/₩₩/₩₩₩+` 필터가 표시되고 가격 정보가 없는 곳은 가격 필터에서 제외되는지 확인한다.
3. 국내에서 `영업 중만`을 처음 누르면 확인 중 표시 후 영업 중인 곳만 남는지 확인한다.
4. 카드의 `관심 없음`을 누르면 사라지고 새로 검색해도 숨김이 유지되는지 확인한다.
5. `잘못된 결과`에서 메뉴 불일치·음식점 아님·폐업/이전·중복 결과 사유를 선택할 수 있는지 확인한다.
6. 상단 `숨긴 곳` 버튼으로 현재 검색의 숨김을 복원할 수 있는지 확인한다.
7. 국내·해외 지도를 움직인 뒤 `이 지역 재검색`을 눌러 지도 중심으로 결과가 바뀌고 지도 화면으로 돌아오는지 확인한다.

### v15 기능 회귀

1. 결과 수 바로 아래에 `목록/지도` 전환이 보이는지 확인한다.
2. 지도 모드에서 1등 추천 카드 바로 아래에 지도 안내와 지도가 나오는지 확인한다.
3. 목록 카드의 `지도에서 보기`를 누르면 해당 핀 정보창이 열리고 지도 영역까지 자동 이동하는지 확인한다.
4. 지도 핀을 처음 누르면 이름·평점만 보이고 목록으로 이동하지 않는지 확인한다.
5. 같은 핀을 한 번 더 누르면 목록으로 전환되어 해당 카드가 강조되는지 확인한다.
6. 현재 위치는 파란 원, 지정 위치는 보라색 마름모로 계속 구분되는지 확인한다.

### v13 기능 회귀

1. 국내에서 `얼큰한 국물`을 입력하면 8개 이상의 구체 메뉴 후보와 직접 입력칸이 뜨는지 확인한다.
2. `짬뽕` 후보를 누른 뒤 주문서와 장소 결과가 짬뽕 한 메뉴에 집중되는지 확인한다.
3. `짬뽕`처럼 처음부터 구체 메뉴를 입력하면 후보 선택 없이 바로 검색되는지 확인한다.
4. 국내 카드에 진짜 후기가 최대 6개, 해외 카드에 현지 최대 4개와 한국인 최대 4개까지 보이는지 확인한다.

### v12 기능 회귀

1. 검색 결과에서 서로 다른 3곳을 `비교 담기`로 선택하고, 네 번째 선택이 막히는지 확인한다.
2. `비교하기`에서 별점·거리·후기 근거·메뉴 적합도·주소가 나란히 보이는지 확인한다.
3. `후보 공유`에서 선택한 후보 또는 상위 3곳의 이름·점수·지도 링크가 포함되는지 확인한다.
4. 맛집을 즐겨찾기에 저장한 뒤 새 폴더를 만들고 해당 폴더로 이동한다.
5. `다녀왔어요`와 방문 메모를 저장하고 새로고침 후에도 남는지 확인한다.
6. 기존 v11 즐겨찾기가 `기본` 폴더에 정상 표시되는지 확인한다.

### 검색 품질 회귀

아래 순서로 한 번씩 확인한다.

1. 국내에서 위치를 `판교역`, 검색어를 `오징어 들어간 짬뽕`으로 검색한다.
2. 주문서에 `오징어짬뽕 → 해물짬뽕 → 짬뽕` 순서가 보이는지 확인한다.
3. 공화춘 후기나 지역화폐 목록 글이 신승반점·조박사짬뽕의 진짜 후기로 나오지 않는지 확인한다.
4. 정확 메뉴 증거가 없으면 `정확 메뉴 중 1등` 대신 `비슷한 메뉴 중 추천`이라고 표시되는지 확인한다.
5. 진짜 후기 0~1개인 장소가 `별점 자료 부족`으로 표시되는지 확인한다.
6. 별점 카드에 `후기 만족도` 막대가 표시되는지 확인한다.
7. 국내 검색 도중 해외 탭으로 바꾼 뒤 다시 검색해 앱이 잠기지 않는지 확인한다.
8. 해외에서 `오사카 난바 라멘`을 검색한다.
9. 구글 원평점과 임슐랭 종합 별점이 구분되어 표시되는지 확인한다.
10. 즐겨찾기에 저장한 뒤 즐겨찾기 화면에서도 별점이 남는지 확인한다.

## 6. 문제 발생 시 되돌리기

GitHub의 v17 Pull Request에서 **Revert**를 누르면 배달앱 기능만 되돌리고 운영 v16을 유지할 수 있다. `v17-delivery-links`, `v16-result-controls`, `v15-map-layout`, `v14-map-list-link`, `v13-review-menu-picker`, `v12-family-tools` 브랜치는 삭제하지 않는다.

## 7. 비용 변화

- 국내 만족도 분석: 규칙 기반이므로 Claude 호출 추가 없음
- 국내 후보: 정확 메뉴부터 검색하고 블로그 후기 수집 대상을 최대 12곳으로 줄임
- 국내 광고 판정: 기존과 동일하게 애매한 후기 최대 40건을 Haiku 1회로 묶음
- 해외 리뷰 번역: 검색어마다 호출하던 방식을 첫 검색어에만 호출하도록 축소
- 메뉴 후보 클릭: 최초 키워드 변환을 재사용하므로 추가 Claude 호출 없음
- 네이버·구글 후기 원자료 확대: 응답량만 늘고 API 호출 횟수는 동일
- AI 심사평·주문 도우미: 기존처럼 1등 또는 버튼 클릭 시에만 호출
- 지도 기준점 구분과 핀·목록 연결: 기존 지도 객체를 재사용하므로 API 호출 추가 없음
- 정렬·가격 필터·숨김: 기존 결과와 기기 저장소만 사용하므로 API·AI 호출 추가 없음
- 국내 `영업 중만`: 사용자가 누를 때만 Google Places 영업시간을 장소별 조회하고 하루 동안 캐시
- `이 지역 재검색`: 새 위치의 장소·후기는 다시 조회하지만 기존 메뉴 변환을 재사용하므로 Claude 키워드 호출 추가 없음
- 배달앱 연결: 기존 결과와 검색어만 사용하므로 API·AI 호출 추가 없음
