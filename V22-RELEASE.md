# v22 함께 쓰는 도구

## 새 기능

- **우리 뭐 먹지?**: 실제 검색 후보 2~5곳을 모임방에 담고 최대 6명이 투표합니다. 미응답은 동의로 세지 않으며 모든 참여자가 수용한 후보만 방장이 확정합니다. 예약·결제는 하지 않습니다.
- **메뉴판 찰칵**: 최대 5장의 메뉴 사진을 한 번에 Haiku 4.5로 읽습니다. 중복 메뉴를 합치고 서로 다른 가격·통화는 확인을 요청합니다. 확인한 메뉴로 인원·예산별 주문 계획과 현지어 주문 카드를 만듭니다.
- **어디서 만날까?**: 기존 검색에서 가져온 실제 식당 후보와 2~6개 출발지의 직선거리를 비교합니다. 가장 먼 참여자의 거리가 짧은 후보부터 정렬합니다. 대중교통·이동시간·전체 지역 최적 장소를 보장하지 않습니다.

## 사진과 비용

- 원본 장당 20MiB, 최대 5장. 촬영과 앨범 선택을 나눴으며 원본은 브라우저에서 순차 최적화합니다.
- 전송 전 JPEG 긴 변 최대 1,568px, 사진 합계 3MiB, 최종 JSON 4,200,000바이트. 서버도 형식·본문·해상도를 다시 검사합니다.
- HEIC/HEIF는 브라우저가 지원할 때만 변환합니다. 지원하지 않으면 JPEG·화면 캡처를 안내합니다. 실제 휴대폰 기종별 호환성은 다를 수 있습니다.
- 실제 분석 버튼을 누를 때만 유료 호출합니다. 사진 파일·판독 메뉴는 앱 저장소에 남기지 않으며, 토큰 사용량과 분석 시도 횟수만 집계합니다. Claude 제공자의 데이터 처리 정책은 별도로 적용됩니다.
- **기본 월 20회는 사진 장수가 아닌 분석 요청 횟수**입니다. 운영·미리보기를 합산하고 UTC 달력 기준으로 초기화합니다. 최대 5장씩 20회이며 실패·시간 초과도 이미 비용이 발생했을 수 있어 한도를 돌려주지 않습니다.
- `MENU_MONTHLY_LIMIT`은 0~100으로 조정 가능하며 기본 20입니다. `MENU_PHOTO_ENABLED=0`이면 AI 분석을 중지합니다. 직접 입력·예산 계산은 계속 쓸 수 있습니다.
- 표준 Haiku 4.5 가격과 최대 출력 기준으로 20회 추가 비용은 대략 US$0.8 규모의 추정입니다. 사진·출력 길이, 환율·세금에 따라 달라지고 **기존 검색 비용을 합친 월 5천 원을 보장하는 한도는 아닙니다.**
- 저장소 연결이 없거나 한도를 확인할 수 없으면 유료 분석을 시작하지 않습니다. 자동 유료 재시도는 없습니다.

## 저장소와 개인정보

- Vercel의 이 앱에 전용 Upstash Redis **Free** 저장소 `matjib-v22-tools`를 연결했습니다. 선택 당시 월 500,000 명령 한도이며 다른 앱의 저장소는 사용하지 않습니다. 유료 요금제로 전환하지 않았습니다.
- 환경변수 `KV_REST_API_URL`, `KV_REST_API_TOKEN`을 서버에서만 사용합니다. 저장소 비밀값을 소스·브라우저·링크에 넣지 않습니다.
- 모임방에는 닉네임·모임 제목/시간·공개 식당 후보·투표·토큰 해시를 저장합니다. 본인 토큰은 참여한 탭의 sessionStorage에만 보관합니다. 탭을 닫으면 권한 복구가 어려울 수 있습니다.
- 초대 링크를 아는 사람은 참여할 수 있습니다. 민감한 정보나 연락처를 모임 제목에 쓰지 마세요. 최대 6명이며 48시간 만료·방장 삭제를 지원합니다.
- 운영과 미리보기 모임방은 별도 이름 공간을 씁니다. 동시 참여·투표·확정은 원자적 비교/저장으로 충돌을 확인합니다.
- 출발지 좌표는 브라우저 메모리에서만 계산합니다. 식당 후보를 넘길 때 식당 공개 필드만 10분 동안 기기 저장소에 담고 한 번 읽은 뒤 지웁니다.
- 모임 화면은 보일 때만 30초마다 갱신합니다. 확정 후 중지하며 오류 시 대기 시간을 늘리고 만료·권한 오류 시 자동 조회를 중지합니다.
- 공유 요청 제한은 무료 저장소 사용량 자체를 완벽하게 막는 방화벽은 아닙니다. 대량 공격·무료 한도 소진 시 새 도구를 일시적으로 사용할 수 없으며, 자동 유료 전환 없이 Free 플랜을 유지해야 합니다.

## 유지보수와 롤백

- 기존 검색 화면 상단에서 새 도구를 열 수 있습니다. 결과 상단과 비교 창에서는 해당 후보를 새 탭으로 전달합니다. 원래 검색 탭은 그대로 둡니다.
- 새 운영 화면은 `/tools/`, 새 API는 `/api/rooms`, `/api/menu-photo`입니다. 로컬 미리보기는 `node preview/v22/server.mjs`로 유지합니다.
- 기존 `api/lib` 공통 파일 3개에 `_` 접두사를 붙였습니다. 함수 수에 포함되지 않도록 한 이름 변경이며 기존 검색 판단 로직은 바꾸지 않았습니다.
- 이전 운영 기준은 **`5305218a8409c51334f926164c453bd81a8a6a44`** (v21)입니다. 이 변경 PR을 병합한 뒤 되돌리려면 GitHub에서 병합 PR의 **Revert → Create pull request → Merge**를 이용합니다. 또는 Vercel에서 이 커밋의 이전 Production 배포를 찾아 **Instant Rollback**을 사용합니다.
- 롤백해도 새 Redis 환경변수가 기존 검색 동작에 영향을 주지는 않습니다. 모임 데이터는 48시간 뒤 만료됩니다. 저장소를 즉시 삭제할 필요가 없습니다.

## 검증 상태

- 로컬 자동 검사 226개 통과(기존 검색 회귀 검사 포함).
- 2026-09-13 Vercel 미리보기 배포 `ce104ce`가 **Ready**, 함수 10개, Node.js 24임을 확인했습니다. **main에는 아직 병합하지 않았으며 운영 사이트 반영·운영 QA는 대기 중입니다.**
- 가상으로 만든 메뉴판 사진 2장을 실제 Haiku API에 한 요청으로 전송해, 메뉴 6개와 USD 가격 판독·주문 조합·현지어 주문 카드까지 확인했습니다. 이는 모의 AI 응답이 아닌 실제 API 검증 1회이며 공유 월 한도에 포함됩니다.
- 같은 브라우저의 두 탭에서 방장·참여자 역할로 영속 Redis 모임방 생성·참여·투표·전원 합의·최종 확정과 다른 탭의 확정 상태 동기화를 확인했습니다. 서로 다른 실기기 간 검증과는 구분합니다.
- 기존 해외 검색에서 도쿄역 라멘 후보 20곳과 평점·후기 표시를 확인했으며, 검색 후보 3곳을 새 탭의 모임 생성 화면으로 가져오는 흐름을 확인했습니다.
- 해외 검색 후보 20곳을 거리 비교 화면의 새 탭으로 가져왔습니다. 테스트 출발지 `(35.68, 139.76)`, `(35.69, 139.78)`를 직접 입력해 실제 식당 20곳의 직선거리 순위와 참여자별 거리 표시를 확인했습니다. 실제 교통 경로·이동시간 검증은 아닙니다.
- 국내 검색은 미리보기 도메인의 카카오 등록 도메인 제한으로 SDK 로드가 실패했습니다. 운영 도메인에서 병합·배포 후 별도 확인해야 합니다.
- 휴대폰 실기기 카메라·HEIC/HEIF·사진 회전은 아직 검증하지 않았으며 사용자 기기에서도 최종 확인이 필요합니다.

## 공식 근거

- [Claude 가격](https://platform.claude.com/docs/en/about-claude/pricing)
- [사진 입력 크기·토큰](https://platform.claude.com/docs/en/build-with-claude/vision)
- [Upstash 가격](https://upstash.com/pricing/redis)
- [Upstash 요청 제한 명령 비용](https://upstash.com/docs/redis/sdks/ratelimit-ts/costs)
- [Vercel 보조 파일 처리](https://vercel.com/docs/functions/configuring-functions/advanced-configuration)
