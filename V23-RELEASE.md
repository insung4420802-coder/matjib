# v23 메뉴판 여러 장 분석 시간 초과 개선

## 확인한 원인

2026-09-13 운영 `/api/menu-photo`의 504 기록에서 실행 시간 35.15초를 확인했습니다. 당시 코드의 AI 중단 제한은 35초였습니다. 사진 파일 용량이 작아도 메뉴 수·글자량·번역 결과가 많으면 이 시간 안에 끝나지 않을 수 있습니다. 파일 크기 제한 오류(413)가 아니라 실제 분석 대기 시간 초과(504)였습니다.

## 변경 내용

- Haiku 분석 최대 120초, 핸들러 작업 예산 130초, Vercel 함수 최대 150초, 브라우저 대기 최대 165초로 안쪽부터 순서대로 여유를 둡니다. 현재 프로젝트의 Fluid compute 설정에서 적용합니다. 다른 API 대기는 기존 45초를 유지합니다.
- AI 응답에서 메뉴마다 반복하던 필드명을 없애고 5칸 배열로 간결하게 전달받습니다. 서버가 기존 메뉴 객체로 엄격하게 검증·변환하므로 화면 데이터 형식은 유지됩니다.
- 사진별 메뉴 출처, 읽기 어려운 가격, 가격·통화 충돌, 메뉴 누락 안내를 계속 검증합니다. 분석 한도는 사진 5장, 페이지당 20개·전체 60개로 동일합니다.
- 분석 버튼 아래에 실제 경과 시간과 대기 안내를 표시합니다. 완료 비율을 추정해서 보여주지는 않습니다. 시간 초과가 나면 같은 위치에 오류를 표시하고 선택한 사진을 유지합니다.
- 사진·메뉴·키·IP 없이 버전, 응답 상태, 처리 단계, 사진 수, 소요 시간만 진단 로그에 남깁니다.

## 비용과 사용 안내

- 기존 Haiku 4.5, 요청당 API 호출 1회, 최대 출력 6,000토큰, 앱 전체 기본 월 20회 제한을 유지합니다. 자동 유료 재시도는 없습니다.
- 기다리는 시간 확대가 총비용이 완전히 같음을 뜻하지는 않습니다. 성공한 분석의 실제 토큰량·서버 사용량에 따라 비용은 달라집니다. 기존 검색을 합친 월 5천 원 결제 상한을 보장하지 않습니다.
- 사진이 많으면 결과를 받을 때까지 같은 탭을 열어 두세요. 새로고침·화면 종료 시 사진과 결과를 복구하지 못할 수 있습니다.
- 제공자 오류나 네트워크 문제까지 모두 없애는 수정은 아닙니다. 계속 실패하면 선명한 사진 1~2장으로 줄이거나 직접 입력할 수 있습니다. 실패한 호출도 이미 과금되었을 수 있어 월 한도는 환급하지 않습니다.

## 검증

- 기존 검색을 포함한 로컬 자동 검사 250개 통과.
- 가상 시간 검사로 35초 이후에도 분석이 계속되어 40초에 성공하는 경우, 120초에 중단하는 경우, 중복 호출이 없는 경우를 확인했습니다.
- 모의 응답 검사로 사진 4장·메뉴 60개의 페이지·가격 검증과 기존 응답 호환성을 확인했습니다.
- 실제 배포에서 글자가 많은 합성 메뉴판 사진 4장으로 별도 확인 후 PR에 결과를 기록합니다. 사용자가 실패했던 원본 사진 및 휴대폰 실기기 검증과는 구분합니다.

## 롤백

수정 전 운영 버전(v22)은 **e5d4f19e2fd0b070a18a3b3670ee417aa0e372ad** 입니다. 이번 수정 PR을 병합한 뒤 GitHub에서 **Revert → Create pull request → Merge**하면 v22 코드로 되돌릴 수 있습니다. 긴급 복구는 Vercel에서 해당 커밋의 이전 Production 배포를 찾아 **Instant Rollback**을 사용합니다. 저장소·환경변수·기존 사용자 데이터는 변경하지 않습니다.
